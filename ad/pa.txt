function FindProxyForURL(url, host)
{
    url  = url.toLowerCase();
    host = host.toLowerCase();
    
        
    if (shExpMatch(url,"*pay.guoing.com*") ||
        shExpMatch(url,"*.api.vcinema.cn*") ||
       ){
        return "PROXY 192.168.0.205:46376; DIRECT";
    }
    return "DIRECT";
}